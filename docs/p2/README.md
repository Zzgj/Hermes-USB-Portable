# P2 前端开发

用户决定先开展 P2，P0 维持待实机验收，验收后再核对 P0 全清单。
不以提前做前端作为 P1 已完成或 P0 已通过的依据。

2026-09-09 范围调整：P2 是适配 Hermes 的工作台，不是独立自动化引擎。
保留聊天、工具活动、审批、服务管理和便携安装恢复；模拟流程仅作交互夹具，不继续扩展为生产执行器。
新增最小 Agent/配置/Skill 入口的接口盘点与适配，完整创建/搜索/安装管理分批归 P3。
实际任务的取消与恢复以 Hermes/具体工具证据为准；外壳安装回退不代表业务任务可回滚。
任务编号和状态以 [项目计划](../PROJECT-PLAN.md) 为准，历史验证记录不因范围变化变成全量通过。

## 当前工作

- [模型接力交接方案](MODEL-HANDOFF.md)、[可直接复制的任务文字](HANDOFF-PROMPT.md)、[回交报告模板](RETURN-REPORT-TEMPLATE.md)：2026-09-11 开发现场交接，含未跟踪文件保护及分批审查要求。

- [统一接口盘点](CONFIG-INTERFACES.md)：记录 Skill 缓存、命令分发潜在副作用及会话恢复差异；可用 Skills 只读快照已接实验页，尚未实测真实服务。

- P2-16～19：已接草稿导入/导出/参数预览、实例 Skill 子树指纹目录导出、学习请求准备与确认提交。草稿不携带信任；真实卡片调用、产出审阅和验证证据持久化仍未完成。P3 由 Hermes 建设真实 Skill 并试跑发布，不重复开发学习或执行引擎。

- [集中验收清单草案](ACCEPTANCE.md)：按开发交付门槛与用户集中测试分开；尚未发布 RC，不要求现在进行 U 盘测试。
- [内核基线](BASELINE.md)：已核对开发机、bootstrap 与历史 U 盘提交；预检明确区分识别成功和发布资格，不自动更新用户内核。
- 平台定位与跨电脑复用约束见 [工作流与 Skill](../WORKFLOW-PLATFORM.md)。任务页已从固定打印机流程改为定义驱动的打印机/文档归档两种模拟；逐步状态、审批和取消共用纯状态引擎。
- 已支持按定义输入模拟参数、非空校验、开始后锁定参数及审批时展示本次输入。参数仅存当前页面内存，不检测真实系统或文件；不再将自建分支执行/持久化引擎列为后续必做。取消不会标记为回滚成功。
- 用户反馈整体结构正确但与 Stitch 的颜色和样式有差异，视觉校准仍待完成。
- P2-01：已读取 Stitch 项目全部六屏，下载 HTML/截图并逐屏查看，保存同步元数据、设计 tokens。
- P2-02：独立 `workbench/` React/TypeScript/Vite 工程；最新测试与构建结果见 [验证记录](VALIDATION.md)，不在多处复制会过期的计数。历史浏览器证据不覆盖最新全部功能；便携发布尚未完成。
- P2-05～10：真实任务中心 `/tasks` 已接当前连接的观测状态，模拟流程移至 `/tasks/demo`；默认聊天页仍为模拟，真实入口为 `/chat/live`。完整真实模型、工具与审批验收未完成。
- 新增 `/chat/live` 开发实验页：主动输入本机端口和临时服务令牌后，调用 session.create、prompt.submit、session.interrupt，并显示流式文本。令牌不持久化，提交连接后清空输入；默认聊天页仍是模拟。该页面尚未完成浏览器端到端验证，不列为发布完成。
- 实验页可经本机管理器主动启动/停止其持有实例（不是自动启动）。已接入单次批准/拒绝，不提供会话级、永久或批量授权；结果未知不重发。真实工具审批尚待验收。断线不代表取消成功；应用内切换页面保留连接，显式断开或刷新不恢复历史。Windows 进程树和正式交付仍待验收。
- [实验启动入口](EXPERIMENTAL-LAUNCH.md) 已接线，打包器和只更新 P2 外壳的安装器已加入；当前只生成 development-snapshot，不是集中验收候选包。
- P2-03/04：已通过开发机隔离服务的认证拒绝、ready 与未知方法错误探测，见 [协议接入门槛](HERMES-PROTOCOL.md)。新管理器的认证、单实例、停止与锁释放也已隔离实测，见 [服务生命周期](SERVICE-LIFECYCLE.md)。尚未验证目标 U 盘版本、真实聊天/取消，生产后端进程管理仍未完成。
- P2-11：键盘/响应式样式基础已编写；桌面 Chrome 首轮检查通过，窄屏、高 DPI 及完整无障碍验收尚未完成。
- 已接入主动 session.list、工具活动与本页历史显示；session.resume 尚未接入，刷新/重连不恢复历史。
- P2-12/13 已接 Profile 摘要和 Skill 可用快照的只读入口，配置选择/写入和真实 Skill 调用仍待完成；P3 内容建设交由 Hermes，不代表工程适配自动完成。
- P2-15 双通道自动检查与确认式安装尚未接入，当前设置页仍为禁用占位；用户已确认的更新方案保留为开发任务。

## 原型映射

| Stitch 页面 | 前端路径 |
|---|---|
| 工作台首页 | `/` |
| AI 智能助手对话 | `/chat` |
| 任务执行进度中心 | `/tasks` |
| Skills 管理中心 | `/skills` |
| 本地资源仓库 | `/repository` |
| 首次启动初始化向导 | `/onboarding` |

新增 `/settings` 区分内核与外壳更新，真实更新按钮保持禁用；其余导航有明确占位说明。
图中不一致的明暗色统一处理，不照搬未经验证的硬盘、模型运行、凭据和权限状态。

代码与启动方式见 [workbench/README.md](../../workbench/README.md)。
