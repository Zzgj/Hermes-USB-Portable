# 本机负责人 → 公司开发者 · P2 交接单

## 2026-09-21 保存补充（优先于下文旧现场描述）

用户已授权本次 Git 保存/推送，交接分支为 `handoff/p2-local-checkpoint-20260921`。
它从本机 aecd17d 保存完整 WIP、五个新增代码/测试文件和整理后的 docs；精确 SHA 查该分支 HEAD。
本轮 fetch 确认原远端 feat/portable-initializer 仍在 dff159b，该分支不移动、不强推。
这是两条并行代码线的保存，不是整合或审查通过；S0 的现场保存步骤已执行，统一基线仍待处理。
接手时先获取此交接分支，再在独立集成分支审查两线差异。当前构建失败如实保留，不发布包。

更新：2026-09-20。用户已选择双 Hermes 开发协作；本轮只准备提示词、文档和现场核查，
没有创建角色、发送任务、修改个人 Hermes 配置、提交 Git、合并远程或发布测试包。
本机负责人的创建提示词见 [AI_GUIDE](../AI_GUIDE.md)。整体项目状态见 [总计划](../project_plan.md)。

## 1. S0：先保护与统一开发现场（首项，未完成）

本机目录：`/home/zgj/桌面/CodeRepository/Hermes-USB-Portable`。
跨电脑不能照抄此绝对路径，先核实接手机器的实际仓库。

| 现场 | 身份与状态 |
|---|---|
| 本机 HEAD | aecd17d22e225e1ecae7b9ce3744cdad8218c8e3，feat/portable-initializer |
| 本机 WIP | 任务卡片准备/二次核验、观测报告，以及未完成的任务→学习传递；不是已提交代码 |
| 本地 origin 跟踪引用 | dff159bed2c275d1b6a9c7c0c04c5b7bddf58bda；本轮未重新 fetch |
| dff159b | 另一模型 A/B/C/D 交付，前次在独立 worktree 审查；没有合入本机 |
| P0 历史候选 | 540d4442eb60c0f12548e79a274f5ce28deef97f，RC2，最终实机验收未完成 |

本轮文档整理前，本机有 19 个已跟踪文件修改、5 个未跟踪文件。其中原 docs 的三份改动
已并入新结构，不是丢弃；package-policy.mjs 既有 prepare-capability 白名单也须保留。
新增文件不能漏掉：
- workbench/scripts/prepare-capability.mjs
- workbench/src/domain/capability-run.ts
- workbench/src/domain/learning-source.ts
- workbench/tests/capability-run.test.mjs
- workbench/tests/prepare-capability.test.mjs

本轮文档整理前快照：`/tmp/hermes-docs-before-handoff-qSS90pP5/docs`。
仅本机临时恢复用途，不是长期交付物，也不包含代码 WIP 的完整备份。
已提交的旧文档可从 aecd17d 恢复；dff159b 的原回交/进度文档仍在该提交中。

接手顺序：
1. 只读核对 git status --short、git log -1、git branch -vv，比较本机与目标差异。
2. 列出已有 WIP/新增文件，核对敏感信息排除；用户确认后保存独立 checkpoint（可明确标为 WIP）。
   不在脏工作区直接 pull/rebase/checkout；不硬重置，不自动 git add -A 纳入私人文件。
3. 核对可访问的远程当前状态；把审查基线和结果固定为完整 SHA，不审查漂移的分支名。
4. 独立集成分支/worktree 选择性整合两线；重叠模块逐文件比较，不能简单覆盖。
5. 保留本轮 docs 整理，不复活旧的重复 README、MODEL-HANDOFF 和 RETURN-REPORT；
   外部文档有价值的增量归并进本结构。先修到可构建，再重新跑全部门禁。
6. 报告统一后的 SHA/状态，用户确认代码交接范围后才推送给公司 Agent。
   本轮没有自动提交/推送，所以仅克隆远程仍缺本机 WIP 与新文档。

## 2. 当前本机必须先处理的问题

2026-09-20 workbench 中的 npm test：115/115；npm run validate：16 个组件；
npm run build 失败（两处 TypeScript 错误）：
- LiveChatPage.tsx 传入 selectedLearningSource/onLearnInvalidated，但 ServiceControlsProps 未接入。
- LiveTasksPage.tsx 读取 taskCapabilityCopy.learn，但对应文案字段不存在。

这些是先前任务→学习接线的未完成增量，不是 dff159b 引入，也不是本轮文档导致。
不能只删调用或用 any 压掉错误就宣称需求完成：核对选中轮次、来源投影、失效/确认流程并补测试。
原有 dist 即使存在也可能来自旧构建，不得据此发布或验收当前源码。

## 3. dff159b 审查问题（仍待修复/整合后复核）

前次独立 Linux 审查：151/151 测试、16 组件检查及生产构建通过；
未做 Windows/U 盘与真实业务验收。以下结果针对 dff159b，不套用到本机 WIP。

| ID | 级别 | 问题与期望修复 | 最低回归案例 |
|---|---|---|---|
| R1 | P1 | useLiveChat 将 Hermes 端口/令牌用于管理目录 API；管理服务与内核身份必须分离 | 不同端口/令牌的两个真实本机 HTTP 服务，不用同端点假夹具掩盖错误 |
| R2 | P1 | 确认执行仅使用缓存 match；提交前重新读取并核验方法与实例，变化撤销审阅 | 修改/删除 Skill、切换实例、迟到响应、双击确认不误发 |
| R3 | P2 | Settings 定时器固定输出“暂无更新/未检测到入口” | 未接服务明确 unavailable；不得伪造检查结论；故障与无更新区分 |
| R4 | P2 | storage 工具无页面调用，卡片仍只在组件状态 | 页面切换/刷新、存储拒绝、非法记录、跨实例隔离；先确认便携存储方案 |
| R5 | P2 | environmentFingerprint 仅包含 Skill 目录，不能识别机器/依赖变化 | 同 Skill 不同环境不得继承验证；环境未知保持待复验 |
| R6 | P2 | 恢复提示固定 incompleteTools=false，当前时间冒充活动时间 | 状态未知、未完成工具与历史时间正确展示，不自动恢复执行 |

R2 可评估复用本机 WIP 的二次核验方案，但仍须针对统一代码重新审查。
R4 不能只把 localStorage 函数挂进 UI：随机管理端口导致 origin 变化，浏览器存储也不是 U 盘
可迁移记录；本机负责人先决定存储范围/目录/迁移与敏感字段投影，不保存输入、令牌和永久审批。
证据读取接口尚未实现；外部 trusted 标签不能自证可信，模型轮次结束不是业务验证。

## 4. 建议的小批次

| 批次 | 目标 | 提交门 |
|---|---|---|
| S0 | 保存现场、统一基线方案 | 新增文件不丢；每条代码线来源可追溯；用户确认整合方式 |
| S1a | 任务→学习构建与失效边界 | 构建恢复，来源来自明确轮次；断线/换轮次撤销审阅 |
| S1b | 卡片管理接线/重核/真实轮次关联 | R1/R2 回归，双服务集成和合成浏览器证据 |
| S2a | 学习产出与变更审阅 | 生成和验证分离，写入范围明确，不自动发布 |
| S2b | 记录存储/证据/环境 | 先审批字段和位置，再实现；R4/R5，失败关闭与迁移测试 |
| S3a | 更新/备用入口/恢复状态 | R3/R6，确认式安装、真实响应、不支持明确 |
| S3b | Profile/Skill 最小入口与 UI 收尾 | 副作用、权限、视觉/键盘/窄屏矩阵 |
| S4 | 集中交付 | 开发门禁、版本清单/哈希/恢复说明、Windows 待验项完整 |

可以再次拆小，不让一个任务同时横跨 A/B/C/D。具体实施顺序由负责人根据依赖决定，
不得把表格本身当作已经批准使用个人配置、联网安装或真实工具执行。

## 5. 负责人下发模板（复制填空）

```text
任务 ID / 所属 P2 条目：
基线完整 SHA / 目标分支 / 仓库地址（确认可共享）：
目标（一个可独立验收结果）：
允许修改文件/模块：
禁止范围（个人数据、上游源码、锁文件升级、发布等）：
实际接口/输入输出/副作用及版本依据：
安全约束（服务身份、令牌、确认、防重放、存储）：
必须通过的正常/失败/断线/重复请求案例：
允许的测试环境和费用范围：
交付：结果完整 SHA、文件清单（含新增）、证据、未测项和风险。
出现接口差异、新依赖、权限或范围扩大时暂停该部分并报告。
未经允许不推送共享主分支，不自动合并或执行真实实例操作。
```

## 6. 公司 Agent 回交模板

```text
任务 ID：
base SHA → result SHA / 分支：
本轮实际改动（含新增文件）：
前置修改如何保留；有没有改接口/依赖/配置：
实际调用契约、已知副作用：
新增存储的路径、字段、大小限制、脱敏与迁移策略：
命令 | 环境 | 退出码 | 结果 | 未覆盖范围：
- 单元/静态/构建
- 合成浏览器
- 隔离真实接口
- 真实模型/工具（无授权则写未测）
- Windows/U 盘（无环境则写未测）
已知失败/安全边界/仍需决定的问题：
负责人重点审查位置：
本批是否具备审查条件（不是自行宣布发布）：
```

## 7. 本机审查与交付收尾

- 核对完整 diff 和新增文件；不能只看回交报告或计数。
- 独立复跑适用门禁，确认修改只针对批准范围；严重问题返工后再复验。
- 给出通过/返工/待环境验证，以及阻塞依赖关系；代码审查不能替代真实业务验收。
- 更新 PROGRESS-GUIDE/VALIDATION 和总计划，不新建第二份长期进度清单。
- 集中包前确认没有旧构建混入，兼容清单、文档、安装白名单及哈希相互一致。
- Windows 两项历史失败（外部报告的 installer/大小写测试）本轮未复核，Linux 全绿不关闭它们。
- P0 最终验收、P2 Windows 服务树/停止/迁移与真实模型审批仍由用户环境提供证据。
- 本机与公司 Agent 的实例路径、通信/仓库访问权限、公司数据边界尚需配置时核对，
  不需要在本轮为此开放端口或安装通信插件。
