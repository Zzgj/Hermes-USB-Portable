# P0 进度与历史证据

2026-09-20 交接状态：**RC2 待最终集中实机验收**。本轮没有重新做 Windows/U 盘测试。
操作只看 [ACCEPTANCE.md](ACCEPTANCE.md)；实现、日志和初始化规则看 [ENGINEERING.md](ENGINEERING.md)。
历史 RC1 的手工补丁/复制路径不作为当前安装步骤。全项目见 [总计划](../project_plan.md)。

## 任务收口

| 任务 | 状态 | 结论与剩余工作 |
|---|---|---|
| P0-01 仓库/分支 | [x] | 原始 Fork/迁移确认完成；接手仍须重新核对实际 HEAD |
| P0-02 数据排除 | [x] | 已有 Git 排除基线；每次交付继续查凭据/运行数据 |
| P0-03 项目文档 | [x] | 原始方案已落地；2026-09-20 整理为总计划/指南/阶段目录 |
| P0-04 最小初始化器 | [x] | 普通目录、幂等与保留数据基线完成 |
| P0-05 Windows/Unicode | [x] | 双 PowerShell CI 与用户中文 Win10 初始化已有证据 |
| P0-06 Runtime 锁 | [x] | 来源、大小、SHA256 与 bootstrap 身份已记录 |
| P0-07 更新/恢复 | [-] | 官方更新及回执已有证据；RC2 检查点恢复等新增逻辑待集中实机验证 |
| P0-08 主流程不退化 | [-] | CLI/Setup/Doctor/Gateway/日志已有反馈；最终包回归待验 |
| P0-09 首次 Runtime | [x] | Win10/exFAT 核心安装与幂等启动已验，不等于所有前端支持 exFAT |
| P0-10 中断/软重建 | [-] | 损坏回执/缓存、中断续跑、Soft Reset/重建已有证据；最终收口不重复破坏测试 |
| P0-11 多入口 | [-] | NTFS CLI/TUI/Web 已有证据；独立 Web STOP 待最终验证 |
| P0-12 便携迁移 | [-] | E/F、换电脑已验；RC2 新旧会话 cwd 修复需集中验证 |
| P0-13 宿主落地 | [-] | 旧 Gateway/Cua 归属已识别；可选组件边界需验，不声明零污染 |
| P0-14 交付材料 | [-] | RC2 双 Windows CI/构建已过；最终包实机结果尚未收齐 |

## 保留的证据摘要

本页整理用户反馈与 CI 记录，不要求重复执行历史故障测试。
当前集中验收见 [ACCEPTANCE.md](ACCEPTANCE.md)。

## 已有证据

- 损坏 ripgrep 回执：单步重建，其余回执、数据/知识哨兵和 Git 状态保持正确。
  证据后缀：`p0-ripgrep-recovery-20260905-123146/result.json`。
- 损坏 rg.zip：拒绝坏缓存，重新下载并校验，恢复可执行文件和 ready 标志。
  证据后缀：`p0-rg-cache-recovery-20260905-124011/result.json`。
- 下载受控失败与续跑：失败不生成 ready；恢复原锁和缓存后续跑成功。
  测试脚本退出码误判已独立复核：
  `p0-interruption-retry-20260905-124604/result-addendum.json`。
- Soft Reset：删除沙箱 Runtime/源码，数据、知识、日志文件哈希不变；
  `HermesPortable-P0-SoftReset-Evidence-20260905-133623-b5fb7e19`。
  后续使用绝对 Root 重建退出码 0，保留检查及 Hermes 导入通过。
- 官方更新：`update-apply-20260905T090649717Z-91855f95.json`，
  官方及外壳退出码均为 0；提交从 `29112bef` 更新至 `f58fcc81`，
  双方回执一致，Runtime manifest 已更新，声明版本仍为 0.21.0。
  该次大小写冲突通过人工备份绕行，不能算自动处理的实机证明。
- NTFS U 盘 CLI/TUI/Web 收发、安全拔插后聊天、换电脑 F→E 后 Web 收发成功。
  exFAT 上 npm workspace 链接失败，不视为 TUI/Web 支持的文件系统。
- RC2 提交 `540d444` 的 CI run `34047793403`：Windows PowerShell
  5.1、7 和候选包构建均成功；覆盖新增修复、回退和中断边界夹具。

## 不应混淆的边界

- 核心探针通过不证明旧会话 cwd、Web STOP 或全部可选工具均可用。
- 新增修复及内核恢复仍需一次集中实机验收；不宣称 P0 已完成。
- 宿主 Cua 包和旧 Gateway 任务单独记录，不自动删除，不宣称零宿主落地。
- 原始证据保存在用户实例或外部备份，不随源码发布；不要分享密钥、完整
  会话、数据库备份或未经审核的原始日志。


## 下一步

用户具备设备条件后只按 ACCEPTANCE 做变更相关集中验收，提交包哈希、内核提交、环境和失败项。
不要为了补证据执行 Full Reset、格式化磁盘、安装真实驱动或自动清理宿主任务。
