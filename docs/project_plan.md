# Hermes Portable 工作台 · 项目总计划

更新：2026-09-20。这里是整体范围、阶段状态与下一步的唯一入口；任务细节只在所属阶段维护。
首次接手先读 [AI_GUIDE.md](AI_GUIDE.md)，不要把历史测试记录当成当前代码已经通过。

## 2026-09-21 检查点保存

用户已授权保存并推送。当前交接内容保存到独立分支 `handoff/p2-local-checkpoint-20260921`，
精确提交以该分支的 Git HEAD 为准。本轮 fetch 后原远端 `feat/portable-initializer` 仍为 `dff159b`，
不移动该分支、不强推、不合并外部交付。下文未提交状态是整理时的历史快照；
交接时应读取检查点分支，不能只克隆原开发分支。两线统一与构建修复仍未完成。

## 项目是什么

**Hermes 的 Agent/配置/Skills + 适配 Hermes 的 Web 工作台 + U 盘便携外壳。**

目标：一次成功，多次复用；一张已验证能力卡片对应一项有证据、适用范围明确的方法。
Hermes 负责规划和工具执行；工作台负责输入、真实状态、审批与证据展示；外壳负责运行时、
目录迁移、服务和程序更新。不另建 DAG/工作流执行引擎，不承诺通用业务回滚。
打印机只是可选案例，首个试点使用测试文件整理。远程 API 优先，不集成本地模型 Runtime。

## 阶段与唯一入口

| 阶段 | 状态 | 详细任务/材料 |
|---|---|---|
| P0 便携基线 | RC2 已有自动回归及历史实机证据，最终集中验收未完成 | [进度与证据](p0/PROGRESS.md)、[验收](p0/ACCEPTANCE.md) |
| P1 配置、凭据与知识 | 整体未完成；接口盘点作为 P2 依赖按需推进 | [任务](p1/PLAN.md) |
| P2 Web 工作台 | 开发整合中，非 RC；两条代码线待统一，本机 WIP 构建失败 | [当前进度](p2/PROGRESS-GUIDE.md) |
| P3 Agent/Skill 内容与复用 | 方案已确认，独立配置、试点和跨机复验待开展 | [任务与边界](p3/PLAN.md) |
| P4 发布成熟化 | 尚未开展 | [任务](p4/PLAN.md) |

状态：`[x]` 有相应完成证据；`[-]` 实现中；`[ ]` 未开始；`[!]` 需用户授权/环境。
不按代码行数、测试数量或模型自报推算百分比；“代码已写”“自动测试通过”“实机验收通过”分别记录。

## 现在的代码现场（交接快照，不是永久分支状态）

- 本机分支：`feat/portable-initializer`；HEAD：`aecd17d22e225e1ecae7b9ce3744cdad8218c8e3`。
- 本机有未提交/未跟踪的 P2 增量及本轮文档整理，不能硬重置或直接覆盖。
- 2026-09-20 检查的 origin 本地跟踪引用：`dff159bed2c275d1b6a9c7c0c04c5b7bddf58bda`。
  本轮未重新 fetch，不声称这就是服务器此刻最新提交。
- `dff159b` 是另一模型的独立交付，已审查但未整合；问题与批次见 [交接单](p2/HANDOFF.md)。
- `workbench/hermes-compatibility.json` 仍为 `release_ready:false`。
- 2026-09-20 本机：115 项测试通过、16 个组件检查通过，构建有两处 TypeScript 错误。
  另一提交此前的 151 项通过不能替代本机验证。

## 下一步顺序

1. 本机 Hermes 保护完整 WIP（含新增文件），核对远程，建立唯一可交付的开发基线。
2. 优先修复构建和卡片真实接线/提交前重核；每个小批次由公司 Hermes 开发、本机 Hermes 审查。
3. 完成学习产出、验证记录、配置/更新入口及 UI 收尾；真实状态未知时明确显示未知。
4. 满足 [P2 交付门槛](p2/ACCEPTANCE.md) 后生成一个集中测试包、可信哈希、安装/恢复说明。
5. 用户有设备后分别完成 P0/P2 集中验收；不要反复要求零散补丁测试。
6. P3 用独立配置开展低风险方法复验；真正驱动/AD 等操作单独授权。

## 已确认决策与授权边界

- 自有工作台为主；原 Web/CLI/TUI 为备用，保留原启动器。
- 首次安装基线和后续更新分开；内核复用经核实的上游更新器，外壳独立安装/恢复，
  Runtime 按锁校验。声明版本相同不代表提交/协议相同。
- 更新自动检查、通知；安装须确认。兼容性未知不能静默应用；不修改清单来伪造发布资格。
- Agent/Skill 内容由 Hermes 在用户指定的独立实例配置；提示词不是权限沙箱。
- 2026-09-20 起开发协作采用本机负责人 + 公司开发者。负责人批准代码交付，
  不等于批准管理员操作、模型费用、安装、发布、私人数据传输或真实业务执行。
- 方案调整、自动通信、公司数据使用和代码推送范围须得到对应授权；本轮只整理材料，
  不配置 Hermes 实例、不自动下发消息、不提交/推送 Git、不发布包。
- 旧 ADR 的有效边界并入 AI 指南；历史全文保留于 Git，不继续维护重复进度文档。
