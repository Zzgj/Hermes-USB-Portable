# P3：Hermes 配置集成、Skill 复用与场景验证

更新：2026-09-20。配置与试点尚未执行。开发协作的“双 Hermes”不代表产品内必须实现多 Agent 平台。

## 目标

通过 Hermes 配置、提示词、Skills 和已有工具/MCP 完成工作并沉淀可复用方法。
工作台负责适配与展示，不实现独立工作流执行引擎。方法与平台边界见下文。
打印机只是可选试点；两个不同场景的跨电脑复用仍需实际验证，不把驱动仓库作为平台先决条件。

## 执行分工（用户已确认）

- **Hermes 负责内容建设**：在用户指定实例内提出并配置工作流 Agent 的提示词、Skills 与工具/MCP 方案，执行低风险试点并沉淀方法。
- **工程开发 Agent 负责工程适配**（当前由本机 Hermes 审查、公司 Hermes 开发，Codex 可按需复审）：P0/P2 外壳、接口盘点、UI 接线、诊断与验收支持；不在本仓库重复配置另一套工作流 Agent，不改个人 Hermes 配置。
- **用户负责审批与验收**：确认配置目标、变更方案、第三方安装及权限；决定草稿是否发布，提供跨电脑场景验证。
- 这是一项任务分工，不是已完成声明，也不意味着本次立即调用 Hermes、安装 MCP 或修改配置。
- Hermes 配置前提出方案并备份；优先单个 Agent 的小闭环，不预设复杂多 Agent 架构。提示词不能替代程序权限控制。

| 任务组 | 内容/配置主责 | 工程主责 |
|---|---|---|
| P3-01、04、05 | Hermes 配置角色、执行规则与 Skill 方法 | 工程 Agent 核实接口、便携路径与数据边界 |
| P3-02、03、06 | Hermes 生成草稿、推荐候选；用户审核安装/发布 | 工程 Agent 适配查看、选择和必要管理 UI，不自建 Skill/MCP 系统 |
| P3-07～09 | Hermes 在用户批准的环境执行；用户核对结果 | 工程 Agent 排查适配问题，不代替实机验收 |
| P3-10、11 | Hermes 提供脱敏方法与验证记录 | 工程 Agent 关联实际记录与版本、完善界面 |
| P3-12～14 | Hermes 提炼 Skill 并执行复验，用户审核发布与知识归档 | 工程 Agent 适配卡片、学习入口和验证证据；不代写另一套 Agent |

## 任务清单

本次重排 P3 编号；旧条目被替代而非完成，历史保留在 Git；全项目现状见 [总计划](../project_plan.md)。

- [ ] **P3-01** 按 P2-12 接口盘点配置 Agent 角色/提示词与工具边界，优先复用 Profile 等上游机制；不新增 Agent 框架。
- [ ] **P3-02** 适配 Skills 的查看、选择、创建草稿与编辑能力；先核实接口，不假定所有管理操作已开放。
- [ ] **P3-03** 适配 Skill 搜索/导入及 MCP 配置、连接检查；安装与新增权限明确确认，不因模型推荐自动信任。
- [ ] **P3-04** 配置“执行→验证→总结→生成 Skill 草稿”提示词和规则；失败结果不能标为已验证方法。
- [ ] **P3-05** 记录方法来源、版本、适用环境、依赖、输入、验证结果和恢复限制；优先使用 Skill 文档/配套脚本，不引入自有流程 DSL。
- [ ] **P3-06** 实现草稿审核与验证状态展示；区分生成、审核和实测通过，不将 trusted 标签视为安全保证。
- [!] **P3-07** 选择两个不同场景，在 A 电脑通过 Hermes/Skills 执行并验证；打印机需专用设备及单独授权，可先用低风险场景。
- [!] **P3-08** 在 B 电脑复用方法，重新检查系统、参数、依赖与权限；不继承 A 的路径、IP、凭据或授权。
- [!] **P3-09** 验证拒绝审批、取消、失败和环境不匹配；恢复能力由具体 Skill/工具决定，不能恢复时提供人工说明。
- [ ] **P3-10** 将真实会话/任务结果与 Skill 版本关联，保存脱敏证据；复用 Hermes 记录能力，不额外复制私人正文。
- [ ] **P3-11** 根据试点暴露的接口差距补 UI 适配；完整 Agent/Skill/MCP 管理分批交付，不要求复制原版全部设置页。
- [!] **P3-12** Hermes 在独立实例完成低风险测试文件整理并验证后，用 `/learn` 提炼 Skill 草稿；审核后重新试跑生成的方法，不能用原对话成功代替 Skill 验证，再发布第一张已验证能力卡片。
- [!] **P3-13** 以第二种方法和 B 电脑检验“卡片→本次参数→Hermes→业务验证→执行记录”闭环；覆盖 Skill 更新、依赖缺失和环境变化导致的重新验证。
- [ ] **P3-14** Hermes 把失败环境、错误、原因确定程度与处理办法整理为脱敏排错参考；用户确认后按需关联 Obsidian/知识库，不默认复制完整会话或自动改写已验证 Skill。

停止扩展：独立流程调度、通用分支执行器、自建权限引擎和通用业务回滚。
按需后置：驱动制品仓库、硬件匹配及打印机专用 UI；仅实际案例需要时评估，不作为第一版必做项。
若需要真实驱动，来源、哈希/签名、许可证和专用环境检查仍不可省略。
Bundle 用于组合加载 Skills，不视为有顺序保障的流水线；必要执行约束写入主 Skill 并实际验证。
Cron/Trigger、完整运维分类库及 Computer Use 扩展按场景后置，不随本次卡片需求全部加入首版。

## P3 验收条件

- 两种不同方法能由 Hermes/Skills 在 A 验证并在 B 重新绑定环境复用；不依赖工作台写死业务流程。
- Skill/MCP 来源和权限经审查，生成草稿与验证通过明确区分。
- 管理员操作的对象、原因和风险在授权前可见。
- 任务可取消；失败有结构化结果、日志和可执行的恢复说明。


## 方法、卡片和知识边界

### 从 A 电脑到 B 电脑

1. 在 A 完成工作，记录操作、前置条件、结果和适用范围。
2. 提取通用方法，将机器相关信息参数化，生成草稿。
3. 人工审核来源、敏感信息、权限范围与恢复策略，发布带版本的定义。
4. 在 B 重新绑定本机输入、探测条件与依赖，生成本次计划。
5. 针对本次目标重新确认权限；A 的批准不能继承到 B。
6. 执行、验证并保存新记录。不匹配应停止、分支或请求帮助，不能假报成功。

打印机可能需要系统、网络、驱动检查；文档归档可能需要选文件、提取、分类、确认输出和写入。
平台不得把打印机的检查顺序套用给其他工作流，也不能硬编码 A 的 IP、路径或凭据。

### 已验证能力卡片与学习闭环

**一张已验证卡片 = 一项有成功证据、适用范围明确、可以重新调用的方法。**
卡片只描述任务目标、输入、Skill/Bundle 引用、版本/内容指纹、环境依赖、验证条件与历史证据。
不把每个命令/节点编码成工作台自己的执行引擎，也不把 Toolsets 声明当成已经获得权限。

推荐入口为“我的能力”“执行记录”“整理为可复用技能”，聊天是执行与解释的基础界面。
过程是：真实任务完成并验证 → 主动发起 `/learn` → Hermes 生成草稿 → 审核 → 试跑该 Skill →
保存证据并形成已验证卡片 → 换环境时重新检查、授权和验证。
原操作成功不能替代生成 Skill 的复验；版本、Bundle 成员或适用环境变化时提示重新验证，
保留历史证据但不将其当成当前版本已通过。失败经验进入排错参考，不自动成为成功卡片。

已核对开发机 `cli_commands_mixin.py` 与 `tui_gateway/methods_tools.py`：`/learn` 构造指导提示，
交给 Agent 通过已有工具整理 Skill，不是录制器或确定性回放引擎。适配仍须验证目标内核契约。
官方参考：[Skills 与 /learn](https://hermes-agent.nousresearch.com/docs/user-guide/features/skills/)。
Bundle 是多个 Skills 的组合加载入口，不保证依次执行；具体顺序、失败处理和业务验证由方法约束。

方法优先使用适合的 API/CLI，Browser/Computer Use 按需补充；不保证 GUI 自动化始终可用。
Obsidian 为可选知识沉淀位置，不作为卡片执行必需条件，不默认归档完整私人会话。

### 安全边界

- 模型生成的流程不是可信可执行代码；进入系统执行前必须验证与审核。
- 凭据用受控引用绑定，不放入可分享的定义或证据。个人信息按需脱敏。
- 审批绑定执行实例、步骤和具体计划；计划变化使旧审批失效。
- 取消不代表撤销副作用。只有实际执行并验证恢复后才能标记回滚成功；不支持时提供人工恢复说明。
- 验证记录注明系统、版本、时间和结果；超出范围时重新检查，不能以 trusted 标签保证成功。

其他阶段状态只看 [项目总计划](../project_plan.md)，不在这里重复维护。
